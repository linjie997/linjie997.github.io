import { DisplayLanguagePipe } from './display-language.pipe';

describe('DisplayLanguagePipe', () => {
  it('create an instance', () => {
    const pipe = new DisplayLanguagePipe();
    expect(pipe).toBeTruthy();
  });
});
